<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:background="@drawable/bg"
    android:padding="5dp" >

    <ImageView
        android:id="@+id/title_image"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/title"
        android:contentDescription="@string/app_name" />
    
    <ImageButton
        android:id="@+id/near_me_btn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:paddingTop="35dp"
        android:paddingBottom="35dp"
        android:src="@drawable/near_button"
        android:background="@null"
        android:contentDescription="@string/near_me_btn" />

    <ImageButton
        android:id="@+id/coming_up_btn"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:paddingTop="35dp"
        android:src="@drawable/coming_button"
        android:background="@null"
        android:contentDescription="@string/coming_up_btn" />

</LinearLayout>